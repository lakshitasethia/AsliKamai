project_id = "wwsggroioiptyhyrylnm"

# The app authenticates with a publishable key (not a JWT); the function
# checks the apikey header itself.
[functions.gemini-proxy]
verify_jwt = false

# Verifies the rider's session token itself (auth.getUser), see index.ts.
[functions.delete-account]
verify_jwt = false
